name: csv-to-csvw example
on:
  push:
    branches:
      - main
jobs:
  generate_csvw_from_csv_upload:
    name: Generate CSV-W from csv upload
    steps: 
    - name: csv-to-csvw action
      uses: GSS-Cogs/csv-to-csvw-action@v0.0.1-alpha
